{
  "extends": "../../tsconfig.json",
  "exclude": ["dist", "node_modules"],
  "compilerOptions": {
    "lib": ["ESNext", "ESNext.AsyncIterable", "DOM", "WebWorker"],
    "types": ["vite/client"],
    "jsx": "preserve",
    "paths": {
      "@/*": ["./src/*"]
    }
  }
}
